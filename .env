############################################################
# 📦 Project Information
# ----------------------------------------------------------
# These variables define image naming conventions.
# In CI environments, they should be provided and overridden
# by the CI container runtime.
# See example: ./.github/workflows/publish-docker.yml
############################################################

# Local image name (Must match package.json 'name' field)
REPO_NAME=template-turborepo-vue

# Full registry path (Normally overridden by CI container)
#REPOSITORY=ghcr.io/pkgtoolsjs/template-turborepo-vue

# Optional image tag (Normally overridden by CI container)
#TAG=1.0.0


############################################################
# 🛠️ Build Configuration (Optional)
# ----------------------------------------------------------
# ⚠️ NOTE:
# 1. This is a reference template – avoid hardcoding secrets!
# 2. Configure sensitive values properly via:
#    - CI/CD: Repo Settings > Secrets and variables
#    - Runtime environment variables
############################################################

# Docker Build output verbosity (uncomment to enable)
#BUILDKIT_PROGRESS=plain

# Privacy - Disable telemetry/analytics (if supported)
DO_NOT_TRACK=1

# TurboRepo team ID and Access token (for remote caching, keep secret!)
TURBO_TEAM=
TURBO_TOKEN=
